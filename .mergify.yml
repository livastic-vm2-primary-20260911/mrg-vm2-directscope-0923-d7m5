merge_queue:
  mode: serial
  max_parallel_checks: 1

queue_rules:
  - name: default
    queue_conditions: []
    merge_conditions:
      - check-success = pr-ci
    batch_size: 1
    batch_max_wait_time: 0
    merge_method: merge

pull_request_rules: []
